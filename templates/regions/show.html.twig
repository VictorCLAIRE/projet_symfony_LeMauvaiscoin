{% extends 'base.html.twig' %}

{% block title %}Regions{% endblock %}

{% block body %}
    <h1>Regions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ region.id }}</td>
            </tr>
            <tr>
                <th>Name_region</th>
                <td>{{ region.nameRegion }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info m-1" href="{{ path('regions_index') }}">back to list</a>

    <a class="btn btn-info m-1" href="{{ path('regions_edit', {'id': region.id}) }}">edit</a>

    {{ include('regions/_delete_form.html.twig') }}
{% endblock %}
