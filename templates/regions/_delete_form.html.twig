<form method="post" action="{{ path('regions_delete', {'id': region.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ region.id) }}">
    <button class="btn m-1">Delete</button>
</form>
